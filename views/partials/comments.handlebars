{{!-- Need to attach the comment details to the post from database --}}

<section class="card">
  <div class="card-body">
      <h3>Comment: {{body}}</h1>
      <p>Posted by: {{user.userName}}</p>
      <p>Date Posted: {{createdAt}}</p>
  </div>
</section>